// ผลลัพธ์ในบรรทัดที่มี * มีค่าเป็นอะไรและเพราะอะไร
let x = 1;
function func() {
  console.log(x); // *              //Error เพราะว่า let ไม่สามารถประกาศหลังจากเรียกใช้งานได้ เพราะมันมีการกำหนดค่าใน scope ทีหลัง ขึ้นว่าใช้งานก่อน initialize 
  let x = 2;
}
func();